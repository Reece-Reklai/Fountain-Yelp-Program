#include <cassert>
#include <cstdlib>
#include <iomanip>
#include <iostream>
#include <map>
#include <string>
using namespace std;

bool Login(map<string, string> Credential);
bool SignUp(map<string, string> Credential);
